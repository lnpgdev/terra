# Terra UI Kit

A framework-agnostic UI Kit built with raw HTML, CSS, and JavaScript. Provides a shared design
language and component library for internal applications.

## Overview

Terra is a core CSS framework and UI Kit designed to ensure consistent styling and behaviour across
all internal applications, regardless of the frontend framework in use (React, Vue, Angular, etc.).
